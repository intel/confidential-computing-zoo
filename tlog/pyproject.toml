[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tlog"
version = "0.1.0"
description = "Trusted Log shared types, ABCs, and digest computation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["tlog*"]
