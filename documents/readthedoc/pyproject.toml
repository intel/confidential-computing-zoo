[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "CloudSecurity"
authors = [{name = "RodgerZhu", email = "rodgerzhu@cloudsecurity"}]
dynamic = ["version", "description"]
